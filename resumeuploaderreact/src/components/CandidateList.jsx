/**
 * CandidateList component for the Resume Uploader frontend.
 *
 * Displays submitted candidate profiles in a table with columns for name,
 * email, DOB, state, gender, location, profile image, and resume file link.
 * Supports delete functionality to remove a candidate entry from the list.
 *
 * @component
 */
